.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install virtual_decoys

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv virtual_decoys
    $ pip install virtual_decoys
